<!DOCTYPE html>
<html>

<head>
    <title>To-Do List</title>
    <link rel="stylesheet" href="style.css">
</head>
<link rel="stylesheet" href="/style_index.css">

<body>
    <h1>To-Do List</h1>

    <div class="todo-container">

        <form id="taskForm">
            <input type="text" id="taskInput" placeholder="Enter a task">
            <button type="submit" id="add_button">Adicionar Tarefa</button>
            <button type="button" id="remove_button">Remover Tarefas</button>
        </form>

        <ul id="taskList">
            <% array_data.forEach((value, index)=> { %>
                <li>
                    <div class="value_task">
                        <%= value %>
                    </div>
                    <div class="buttons_task">
                        <button class="edit_button" onclick="edit_task('<%= value %>')">editar</button>
                        <button class="delete_button" onclick="delete_task('<%= value %>')">excluir</button>
                    </div>
                </li>
                <% }) %>
        </ul>
    </div>
</body>

<script src="/script_index.js"></script>

</html>